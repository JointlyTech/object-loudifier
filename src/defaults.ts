import { $onOptions, Options } from './models';

export const defaultOptions: Options = {
  allowNesting: false
};

export const $onDefaultOptions: $onOptions = {
  preventBubbling: false,
  once: false
};
